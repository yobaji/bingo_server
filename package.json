{
  "name": "bingo_server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "cross-env NODE_ENV=development nodemon server"
  },
  "dependencies": {
    "colyseus": "^0.11.20",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "cross-env": "^6.0.3"
  }
}
